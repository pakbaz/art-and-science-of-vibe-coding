Use `node demo/run.js feedback A|B --trial <id>` from the parent demo repository.
Send its output unchanged. It contains only failures observed in the latest
check and the same workspace-only restriction used for both lanes.
